target = 'lib/libtsload'

Import('env')

lib = env.Clone(LIBS = ['c'])

lib.AddDeps(('lib', 'libjson'), ('lib', 'libtscommon'), ('lib', 'libtsagent'))
env['DEPTARGETS'][target] = lib.SharedLibrary(lib.Target(target), Glob("src/*.c") + Glob("src/disp/*.c"))